/**
 * Phoenix 主页 - 主JavaScript文件
 */

// 页面加载完成后执行
document.addEventListener('DOMContentLoaded', function() {
    // 初始化时钟
    initClock();
    
    // 初始化搜索功能
    initSearch();
    
    // 初始化UI效果
    initUI();
}); 